class Recipe {
	constructor(id, title, image, description, steps) {
		this.id = id;
		this.title = title;
		this.image = image;
		this.description = description;
		this.steps = steps;
	}
}

export default Recipe;
